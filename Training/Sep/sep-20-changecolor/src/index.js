import React from "react";
import ReactDOM from "react-dom/client";
import Cals from "./UseStateExample";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Cals />
  </React.StrictMode>
);
